---
- name: copy artifact
  hosts: localhost
  become: yes

  tasks:
    - name: copy artifcat
      copy:
        src: /mnt/artefact/
        dest: /mnt/artefact/
        remote_src: yes
        owner: root
        group: root
        mode: 0755
